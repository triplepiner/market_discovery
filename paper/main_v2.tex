\documentclass[10pt]{article}

\usepackage[letterpaper,margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage[numbers]{natbib}
\usepackage{hyperref}
\usepackage{xcolor}
\hypersetup{colorlinks=true,linkcolor=black,citecolor=blue,urlcolor=blue}

\title{\vspace{-1.5em}\textbf{Robust Financial PDE Discovery via Gaussian Process Derivatives\\
with Nonlinear KAN Diagnostics}\vspace{-0.4em}}
\author{Makar Ulesov \\ Mohamed bin Zayed University of Artificial Intelligence (MBZUAI) \\
\texttt{makar.ulesov@mbzuai.ac.ae}}
\date{}

\begin{document}
\maketitle
\vspace{-2em}

\begin{abstract}
Data-driven PDE discovery from option market data fails because numerical differentiation amplifies the noise inherent in bid--ask spreads, discrete strikes, and sparse expirations.
We show that replacing finite-difference derivatives with analytical derivatives from a Gaussian Process regression extends the practical noise tolerance of sparse PDE discovery (SINDy) from under $1\%$ to over $10\%$ ($R^2(\text{clean}){=}0.920$ at $10\%$ noise vs.\ $-0.486$ for finite differences).
On $1{,}374$ real option contracts across four equity underlyings, GP-stabilized SINDy recovers interpretable PDE structure consistent with the Black--Scholes family, and a contribution analysis isolates the dominant first-order terms.
A single-layer Kolmogorov--Arnold Network trained on the SINDy-selected terms then acts as a nonlinear diagnostic, lifting in-sample $R^2$ from $0.44$ to $0.93$ and revealing structured deviations from constant-coefficient Dupire --- though out-of-sample maturity transfer fails, indicating non-stationary term-structure dynamics.
The framework also doubles as a misspecification detector, flagging synthetic jump dynamics via spurious term activation.
\end{abstract}

\section{Introduction}

Financial PDE discovery from market data is hard for three specific, currently unsolved reasons. First, option data is noisy: bid--ask spreads create $1$--$5\%$ price uncertainty, discrete strikes limit spatial resolution, and chains rarely contain more than ten distinct expirations, making maturity-direction derivatives unreliable. Second, the canonical Black--Scholes library requires second-order strike derivatives, which amplify any input noise at rate $\mathcal{O}(\varepsilon/h^2)$ under finite differences; this alone destroys sparse regression on real chains. Third, real option dynamics are nonlinear in ways that a fixed linear candidate library cannot capture --- the volatility smile, jump risk, and hedging friction are all PDE-level deviations from constant-coefficient Black--Scholes \citep{blackscholes1973,dupire1994pricing}.

Sparse identification of nonlinear dynamics (SINDy) \citep{brunton2016sindy} and its PDE extensions \citep{rudy2017pde,messenger2021weak} have succeeded in physics but have not been demonstrated robustly on cross-sectional option surfaces. The closest prior work is Feng et al.\ \citep{feng2025feynmankac}, which recovers a stochastic BSDE from single-stock AAPL trajectories via stochastic SINDy under the risk-neutral measure. We differ in two ways: we operate on cross-sectional option \emph{surfaces} (strikes $\times$ expirations) rather than single-stock time series, and we focus on the derivative estimation bottleneck rather than the stochastic regression itself. Mesh-free neural-autograd SINDy \citep{gao2025meshfree} replaces finite differences with autograd through an MLP for physics PDEs; we show below that an analytical Gaussian Process derivative dominates that route on financial data where derivative quality, not function fit, is the bottleneck.

\textbf{Contributions.} (1) We demonstrate that GP analytical derivatives extend the practical noise tolerance of SINDy-based financial PDE discovery from $<\!1\%$ to ${\sim}10\%$, systematically outperforming five alternative derivative estimation strategies on a common benchmark using $R^2(\text{clean})$ --- a metric that separates fit quality from coefficient accuracy. (2) We apply GP-stabilized SINDy to $1{,}374$ real option contracts and recover interpretable PDE structure, identifying dominant first-order dynamics via a contribution analysis that resolves the multicollinearity problem in the standard Black--Scholes library. (3) We introduce KAN activations as nonlinear diagnostics for PDE residual structure, showing that a $[2,1]$ KAN in Dupire log-moneyness coordinates captures structured deviations from constant-coefficient models --- while honestly reporting that these learned nonlinearities do \emph{not} generalize across maturity structure.

\section{Method}

\paragraph{2.1 Problem setup.} In log-moneyness coordinates $k{=}\log(K/F(\tau))$ with forward $F(\tau){=}S_0 e^{(r{-}q)\tau}$, the Dupire equation reads $\partial_\tau C \;=\; c_1\,\partial_k C \;+\; c_2\,\partial_{kk} C$. We construct $C(k,\tau)$ by fitting a per-expiration SVI parameterization \citep{gatheral2014arbitrage} to observed implied volatilities and use the analytical Black--Scholes theta $\theta_i{=}\tfrac{S_0\phi(d_1)\sigma_{\text{imp},i}}{2\sqrt{\tau_i}}{+}rK_i e^{-r\tau_i}N(d_2)$ as the regression target, replacing a finite-difference $\partial_\tau C$ across at most ten irregular expirations. These are deliberate preprocessing choices with inductive bias: SVI imposes a low-dimensional smoothness assumption on each implied-volatility slice, and the analytical theta target assumes local Black--Scholes pricing. Both stabilize the regression but introduce model-dependent bias; we revisit this in \S\ref{sec:threats}.

\paragraph{2.2 GP derivative estimation.} We fit $C(k,\tau)\sim\mathcal{GP}(0,\kappa)$ with an anisotropic RBF or Matern-$5/2$ kernel learned via marginal likelihood maximization, then read off $\partial_k C$ and $\partial_{kk} C$ as conditional means of derivative-augmented kernels \citep{rasmussen2006gp}. This avoids numerical differentiation entirely. The GP posterior mean derivative has approximation error bounded by the posterior standard deviation of the derivative process: for an RBF kernel with length scale $\ell$ and noise variance $\sigma_n^2$, the derivative posterior variance scales as $\sigma_n^2 / (\ell^2 \cdot n_{\text{eff}})$ where $n_{\text{eff}}$ is the effective number of nearby training points. This is qualitatively different from finite-difference error, which scales as $\sigma_n^2 / h^2$ regardless of local data density. The GP therefore provides a natural, data-adaptive smoothing that finite differences cannot match.

\paragraph{2.3 Sparse regression.} STLSQ \citep{brunton2016sindy} on the GP-derived library $\{C,\partial_k C,\partial_{kk}C,k\partial_k C,k^2\partial_{kk}C\}$ identifies the active terms. Standardizing the library columns reduces the condition number on real SPY from $3.4{\times}10^6$ to $61.8$, making sparse regression numerically tractable. We report $R^2(\text{clean})$: the coefficient of determination evaluated against the noise-free target, which separates regression fit quality from coefficient-recovery accuracy.

\paragraph{2.4 KAN nonlinear diagnostics.} The SINDy-selected active terms become inputs to a single-layer Kolmogorov--Arnold Network \citep{liu2025kan}. For Dupire this is a $[2,1]$ KAN $\partial_\tau C = \phi_{\text{drift}}(\partial_k C) + \phi_{\text{diff}}(\partial_{kk} C)$ with two learned univariate B-spline activations. We frame this explicitly as a \emph{diagnostic}: the KAN does not discover a governing equation. It reveals the structured residual between a linear PDE model and the data, visualized through the shape of each learned activation. Under constant-$\sigma$ Black--Scholes both activations are straight lines; nonlinearity in $\phi_{\text{diff}}$ reflects local-volatility structure as a function of the surface's strike convexity.

\section{Experiments}

\subsection{Derivative method comparison}\label{sec:noise}

Table~\ref{tab:noise} reports $R^2(\text{clean})$ for four derivative estimators at representative noise levels on the synthetic Black--Scholes call surface ($r{=}0.05$, $\sigma{=}0.2$, $K{=}100$, $100{\times}100$ grid; full 13-level sweep in Appendix~\ref{app:noise-full}). Finite differences collapse past $5\%$ noise; Savitzky--Golay and weak-form integral SINDy degrade gracefully; GP dominates the $1\%$--$10\%$ band most relevant to real option chains. Beyond $15\%$ the GP smoothing kernel over-regularizes and weak-form integral SINDy becomes the only survivor. Figure~\ref{fig:noise} plots the four methods across the full sweep.

\begin{table}[h]
\centering\footnotesize
\caption{$R^2(\text{clean})$ by derivative estimator and noise level on synthetic BS ($100{\times}100$). Source: \texttt{all\_methods\_noise\_comparison\_v2.csv}, \texttt{gp\_noise\_robustness.csv}.}
\label{tab:noise}
\begin{tabular}{lrrrrrr}
\toprule
Method & $0\%$ & $1\%$ & $5\%$ & $10\%$ & $20\%$ & $30\%$ \\
\midrule
FD             & $1.000$ & $0.816$ & $0.405$ & $-0.486$ & $-1.298$ & $-1.797$ \\
SavGol         & $1.000$ & $0.958$ & $0.846$ & $0.816$  & $0.650$  & $0.345$ \\
Weak           & $0.901$ & $0.898$ & $0.885$ & $0.888$  & $\mathbf{0.832}$ & $\mathbf{0.572}$ \\
\textbf{GP}    & $\mathbf{1.000}$ & $\mathbf{0.990}$ & $\mathbf{0.951}$ & $\mathbf{0.920}$ & $-0.286$ & $-0.974$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]
\centering
\includegraphics[width=0.72\linewidth]{figures/fig1_noise_robustness.pdf}
\caption{$R^2(\text{clean})$ vs.\ multiplicative noise across four derivative estimators on synthetic Black--Scholes. GP dominates the $[1\%,10\%]$ band relevant to real option chains; weak-form integral SINDy is the only survivor above $15\%$.}
\label{fig:noise}
\end{figure}

\subsection{Ablation: what stabilizes discovery}\label{sec:ablation}

To isolate the contribution of each pipeline component we run six configurations on real SPY (Table~\ref{tab:ablation}, Figure~\ref{fig:ablation}). The most informative jump is A$\to$B: replacing finite differences with GP derivatives on a fixed library lifts $R^2$ from $0.167$ to $0.476$. Switching to log-moneyness coordinates with a two-term Dupire library (B$\to$C) costs some $R^2$ on its own ($0.277$) because finite-difference $\partial_\tau C$ across ten irregular expirations remains the dominant noise source. SVI smoothing alone (D, $0.155$) does not rescue this. The analytical-theta replacement (E, $0.288$) lifts $R^2$ above the raw-$K$ baseline, and the $[2,1]$ KAN diagnostic on top (F) almost doubles fit quality to $0.579$. Each successive component contributes asymmetrically: GP is the single largest stabilizer, analytical theta unlocks SVI's benefit, and KAN absorbs the remaining nonlinear residual.

\begin{table}[h]
\centering\footnotesize
\caption{Ablation chain on real SPY (542 contracts, 20260329). Each row adds or substitutes one component. Source: \texttt{ablation\_chain.csv}.}
\label{tab:ablation}
\begin{tabular}{clllllr}
\toprule
Config & Derivatives & Coordinates & Library & $\theta$ source & Model & $R^2$ \\
\midrule
A & FD       & raw $K$           & 5-term & FD         & Linear       & $0.167$ \\
B & GP (RBF) & raw $K$           & 5-term & FD         & Linear       & $0.476$ \\
C & GP       & log-$m$           & 2-term & FD         & Linear       & $0.277$ \\
D & GP       & log-$m$ + SVI     & 2-term & FD         & Linear       & $0.155$ \\
E & GP       & log-$m$ + SVI     & 2-term & analytical & Linear       & $0.288$ \\
F & GP       & log-$m$ + SVI     & 2-term & analytical & $[2,1]$ KAN  & $\mathbf{0.579}$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]
\centering
\includegraphics[width=0.78\linewidth]{figures/ablation_grouped.pdf}
\caption{Ablation chain $R^2$ on real SPY, grouped by intervention. The full chain is non-monotonic: moving from raw-$K$ 5-term (B) to log-moneyness 2-term (C, D) costs $R^2$ on its own --- the GP derivatives and the analytical-$\theta$/KAN pair (E$\to$F) are the two dominant gains, but the intermediate steps reflect that the FD $\partial_\tau C$ remains the bottleneck until analytical $\theta$ is substituted. The KAN-only monotonic story (each component added in the order GP $\to$ log-$m$ $\to$ analytical $\theta$ $\to$ KAN) is shown in Appendix~\ref{app:ablation-kan} (\texttt{ablation\_kan\_clean.pdf}).}
\label{fig:ablation}
\end{figure}

\subsection{Real market data}\label{sec:realdata}

Table~\ref{tab:real} reports the $[2,1]$ KAN-Dupire $R^2$ per ticker on real option chains (542+416+244+172 = $1{,}374$ contracts in total). The KAN lifts in-sample $R^2$ by $+0.32$ to $+0.47$ over linear Dupire on all four tickers, reaching $R^2{=}0.925$ on MSFT and $0.795$ on SPY (the densest chain). The diffusion-edge linearity score ($\phi_{\text{diff}}$ lin $R^2$) lies in $[0.83, 0.96]$ on real data versus ${\ge}0.99$ on synthetic constant-$\sigma$ Black--Scholes, confirming the activation curvature is genuine and not architectural slack. The drift activation remains near-linear across all tickers (lin $R^2{\approx}0.68$), consistent with the canonical $-(r{-}q{-}\tfrac12\sigma^2)$ Black--Scholes drift. The bottom row of the table reports the matching out-of-sample maturity-split $R^2$, which is strongly negative on every ticker --- the in-sample lift therefore reflects expiration-specific shape rather than a stationary operator; we expand on this in \S\ref{sec:threats}.

\begin{table}[h]
\centering\footnotesize
\caption{$[2,1]$ KAN-Dupire vs.\ linear Dupire on real chains (SVI-smoothed surface, analytical-$\theta$ target). $\Delta$ = in-sample lift. Source: \texttt{sindy\_kan\_dupire\_real.csv}.}
\label{tab:real}
\begin{tabular}{lrrrrrr}
\toprule
Ticker & $n$ opts & Linear $R^2$ & KAN $R^2$ & $\Delta$ & $\phi_{\text{diff}}$ lin $R^2$ & Maturity-split $R^2_{\text{oos}}$ \\
\midrule
SPY   & $542$ & $0.439$ & $\mathbf{0.795}$ & $+0.356$ & $0.829$ & $-2.07$ \\
QQQ   & $416$ & $0.401$ & $\mathbf{0.868}$ & $+0.467$ & $0.885$ & $-2.57$ \\
AAPL  & $244$ & $0.432$ & $\mathbf{0.893}$ & $+0.461$ & $0.925$ & $-2.65$ \\
MSFT  & $172$ & $0.604$ & $\mathbf{0.925}$ & $+0.321$ & $0.958$ & $-1.11$ \\
\bottomrule
\end{tabular}
\end{table}

Five-fold spatial cross-validation on SPY confirms a generalization gap that is real but bounded: linear Dupire moves from in-sample $R^2{=}0.44$ to CV $R^2{=}0.29 \pm 0.04$, and the $[2,1]$ KAN moves from $0.77$ to $0.50 \pm 0.07$. The KAN dominates on both metrics, and per-fold activation curves are highly consistent (drift-edge mean pairwise correlation $0.989$, diffusion-edge $0.824$), so the in-sample lift is not a single-fit artifact (Appendix~\ref{app:cv}, \texttt{cv\_results.csv}, \texttt{cv\_kan\_activations\_summary.csv}). The diffusion edge remains the seed/fold-sensitive component, consistent with the activation-stability analysis (Appendix~\ref{app:stability}).

\subsection{Misspecification detection}\label{sec:merton}

Table~\ref{tab:merton} reports SINDy with the Black--Scholes library applied to a synthetic Merton jump-diffusion surface. The canonical $V$, $S\partial_S V$, $S^2\partial_{SS} V$ terms recover values close to their BS counterparts, but the spurious $\partial_{SS} V$ coefficient (true value $0$ under BS) is identified as $\mathbf{1.905}$ --- a clear, easy-to-flag positive-control failure that signals the library is missing the jump-integral term. The framework therefore doubles as a misspecification diagnostic: spurious activation of a term that should be zero is a structural alarm, not a parameter estimate.

\textbf{Quantitative misspecification taxonomy.} Sweeping the Merton jump intensity $\lambda \in [0, 0.5]$ shows that the spurious $\partial_{SS} V$ coefficient scales \emph{linearly} with $\lambda$: a linear fit yields slope $27.88$ with $R^2{=}0.9999$ (Figure~\ref{fig:misspec-jump}), so the magnitude of the spurious activation is a quantitative proxy for misspecification strength rather than a binary flag. At $\lambda{=}0.20$ the spurious coefficient is $5.98$; under a Heston surface with $\sigma_v{=}0.30$ the same term reaches $146.47$ --- a $24.5\times$ magnitude ratio that distinguishes pure-jump from stochastic-volatility misspecification by signature rather than just by presence (Appendix~\ref{app:misspec-taxonomy}).

\begin{figure}[h]
\centering
\includegraphics[width=0.62\linewidth]{figures/misspec_jump_sweep.pdf}
\caption{Spurious $\partial_{SS}V$ coefficient (true value $0$ under BS) vs.\ Merton jump intensity $\lambda$. The linear fit has slope $27.88$ and $R^2{=}0.9999$: spurious activation magnitude is a quantitative misspecification meter. Source: \texttt{misspec\_taxonomy.csv}.}
\label{fig:misspec-jump}
\end{figure}

\begin{table}[h]
\centering\footnotesize
\caption{SINDy on synthetic Merton data with the BS library. $\partial_{SS}V$ is the misspecification flag. Source: \texttt{merton\_discovery.csv}.}
\label{tab:merton}
\begin{tabular}{lrr}
\toprule
Term & BS truth & Merton-fit \\
\midrule
$V$              & $+0.0500$ & $+0.0531$ \\
$\partial_S V$   & $0$       & $-0.0673$ \\
$\partial_{SS}V$ & $0$       & $\mathbf{+1.9049}$ \\
$S\partial_S V$  & $-0.0500$ & $-0.0506$ \\
$S^2\partial_{SS}V$ & $-0.0200$ & $-0.0206$ \\
\bottomrule
\end{tabular}
\end{table}

\section{Threats to Validity}\label{sec:threats}

We list the most important known weaknesses, each backed by an explicit experiment.

\textbf{SVI-induced inductive bias.} The SVI parameterization smooths each implied-volatility slice and may suppress genuine high-frequency structure. Substituting per-expiration cubic-spline IV interpolation collapses $R^2$ to $-0.439$ on SPY; LOESS yields $0.237$; SVI yields $0.288$ (Appendix~\ref{app:smoothing}, \texttt{smoothing\_comparison.csv}). Preprocessing matters, and SVI is the best of the three smoothers we tested --- but the results are not smoothing-invariant.

\textbf{Analytical $\theta$ assumes BS pricing locally.} Replacing the finite-difference $\partial_\tau C$ with the BS theta formula is the single intervention that makes the linear-Dupire regression work on real data, but this is a model-dependent preprocessing step, not model-free PDE discovery. The pipeline therefore discovers structure relative to a BS-consistent target.

\textbf{Maturity non-stationarity --- identifiability collapse, not market signal.} Per-expiration leave-one-out cross-validation on SPY (Appendix~\ref{app:transfer}, \texttt{per\_expiration\_loo.csv}) shows $R^2$ that ranges from $0.81$ at interior maturities ($\tau{\approx}0.80$) down to $-10.57$ at the longest maturity ($\tau{\approx}1.82$); the aggregate maturity-split (train on $\tau{<}0.5$, test on $\tau{\ge}0.5$) is also strongly negative. Crucially, the per-expiration diffusion coefficient does \emph{not} track the market implied-volatility term structure (Spearman corr $= +0.008$) and turns \emph{structurally negative} for $\tau > 0.4$ --- impossible for a local variance. This diagnoses a regression-identifiability collapse from drift/diffusion collinearity at maturity boundaries, not a genuine non-stationary market signal (Appendix~\ref{app:per-exp}, \texttt{per\_expiration\_coefficients.csv}). A complementary moneyness split provides additional evidence of non-invariance: ATM$\to$OTM transfer yields $R^2{=}-0.89$ and OTM$\to$ATM yields $-0.12$ (\texttt{generalization\_analysis.csv}). The learned operator is not stationary across either the term structure or the moneyness regime.

\textbf{Cross-ticker non-transfer.} Training on one ticker and testing on another gives asymmetric results: SPY$\to$QQQ achieves $R^2{=}0.11$ but the reverse QQQ$\to$SPY yields $-1.01$; AAPL$\to$MSFT gives $-1.35$, MSFT$\to$AAPL gives $0.22$; temporal SPY$\to$SPY (March $\to$ May 2026) gives $-3.51$, QQQ $-9.91$, and a milder maturity split ($\tau{<}0.75$ vs.\ $\tau{>}0.75$) gives $-11.01$ (\texttt{transfer\_experiments.csv}). Each underlying carries its own operator; index dynamics do not transfer to single names.

\textbf{Activation stability heterogeneity.} Across five seeds and 200 evaluation points, the drift activation's $95\%$ pointwise band is ${\approx}6\%$ of its dynamic range --- the drift is stable across seeds. The diffusion activation's band is ${\approx}60\%$ of its range --- the diffusion is seed-sensitive (\texttt{activation\_stability.csv}, Appendix~\ref{app:stability}). The drift-edge interpretation is reproducible; the diffusion-edge shape should be reported with explicit uncertainty.

\textbf{Library bias / structured discovery.} The candidate library already resembles the Dupire structure. This is structured discovery with inductive bias, not unconstrained equation discovery.

\section{Related Work}

SINDy \citep{brunton2016sindy} and its PDE \citep{rudy2017pde}, weak-form \citep{messenger2021weak}, ensemble \citep{fasel2022ensemble}, and implicit \citep{kaheman2020sindypi} variants are implemented in \texttt{PySINDy} \citep{desilva2020pysindy}. Mesh-free neural-autograd SINDy \citep{gao2025meshfree} replaces finite differences with autograd through an MLP for physics PDEs; our experiments (\S\ref{sec:noise}) show GP analytical derivatives dominate this route on financial data where derivative quality is the bottleneck. KANs \citep{liu2025kan} were introduced at ICLR 2025; KAN-ODEs \citep{koenig2024kanodes} applied them to dynamical-system identification in physics. The closest concurrent work is Feng et al.\ \citep{feng2025feynmankac}, which recovers a stochastic BSDE from single-stock AAPL trajectories under the risk-neutral measure; we operate on cross-sectional surfaces and address the derivative estimation bottleneck, a complementary attack on the same problem. PINN methods for finance \citep{raissi2019pinn,sirignano2018dgm,dhiman2023option} and the BS, Dupire, Heston, Merton models themselves \citep{blackscholes1973,dupire1994pricing,heston1993closed,merton1976option} complete the picture.

\section{Conclusion}

GP analytical derivatives extend the practical noise tolerance of sparse PDE discovery on option surfaces by an order of magnitude over finite differences, enabling interpretable structural recovery on $1{,}374$ real contracts. A $[2,1]$ KAN-Dupire serves as a nonlinear diagnostic that reveals strike-convex local-volatility structure in-sample, but does not generalize across the maturity dimension --- consistent with the well-documented non-stationarity of long-dated option dynamics. Future work includes term-structure-aware operators, theoretical GP-SINDy error bounds, and application to markets without an established PDE benchmark.

\bibliographystyle{abbrv}
\bibliography{references}

\appendix
\section*{Appendix}

\section{Full noise comparison (six methods $\times$ 13 noise levels)}\label{app:noise-full}
\begin{table}[h]
\centering\footnotesize
\caption{Full $R^2(\text{clean})$ table by method and noise. Source: \texttt{all\_methods\_noise\_comparison\_v2.csv}, \texttt{gp\_noise\_robustness.csv}, \texttt{spectral\_noise\_robustness.csv}.}
\begin{tabular}{lrrrrrr}
\toprule
Noise & FD & SavGol & Neural & Weak & Spectral & GP \\
\midrule
$0\%$    & $1.000$  & $1.000$  & $0.867$  & $0.901$  & $0.941$  & $1.000$  \\
$0.5\%$  & $0.825$  & $0.989$  & $0.866$  & $0.900$  & --       & $0.997$  \\
$1\%$    & $0.816$  & $0.958$  & $0.857$  & $0.898$  & $0.818$  & $0.990$  \\
$2\%$    & $0.791$  & $0.906$  & $0.811$  & $0.895$  & --       & $0.981$  \\
$3\%$    & $0.716$  & $0.871$  & $0.739$  & $0.891$  & --       & $0.972$  \\
$5\%$    & $0.405$  & $0.846$  & $0.557$  & $0.885$  & $0.181$  & $0.951$  \\
$7\%$    & $0.010$  & $0.835$  & $0.369$  & $0.885$  & --       & $0.939$  \\
$10\%$   & $-0.486$ & $0.816$  & $0.119$  & $0.888$  & $-0.477$ & $0.920$  \\
$12\%$   & $-0.720$ & $0.797$  & $-0.020$ & $0.887$  & --       & $0.909$  \\
$15\%$   & $-0.995$ & $0.757$  & $-0.207$ & $0.879$  & --       & $0.120$  \\
$20\%$   & $-1.298$ & $0.650$  & $-0.434$ & $0.832$  & $-0.954$ & $-0.286$ \\
$25\%$   & $-1.534$ & $0.506$  & $-0.800$ & $0.727$  & --       & $-0.700$ \\
$30\%$   & $-1.797$ & $0.345$  & $-1.067$ & $0.572$  & $-1.269$ & $-0.974$ \\
\bottomrule
\end{tabular}
\end{table}

\section{Pipeline}\label{app:pipeline}
\begin{figure}[h]
\centering
\includegraphics[width=0.70\linewidth]{figures/pipeline_diagram.pdf}
\caption{End-to-end pipeline: market chain $\to$ SVI smoothing $\to$ log-moneyness transform $\to$ GP derivative estimation $\to$ SINDy term selection $\to$ KAN nonlinear diagnostic $\to$ out-of-sample validation.}
\end{figure}

\section{PINN validation details}\label{app:pinn}
We train three PINN variants on the discovered BS PDE for sanity-checking the recovered coefficients: (i) baseline soft-constraint \citep{raissi2019pinn,lagaris1998artificial}, (ii) hard-constraint trial function $V{=}(T{-}t)N{+}\max(K{-}S,0)$, (iii) log-price PINN with $x{=}\log S$.

\begin{table}[h]
\centering\footnotesize
\caption{PINN variants on synthetic BS surfaces. Source: \texttt{pinn\_results\_\{call,put\}.csv}, \texttt{hard\_constraint\_pinn.csv}, \texttt{logprice\_pinn.csv}.}
\begin{tabular}{lrrr}
\toprule
Variant & rel.\ $L^2$ & $R^2$ & bdy.\ err. \\
\midrule
Original call         & $1.01{\times}10^{-2}$ & $0.9998$ & --- \\
Hard-constraint call  & $3.08{\times}10^{-2}$ & $0.9985$ & $0.0$ \\
Log-price call        & $4.75{\times}10^{-3}$ & $1.0000$ & $0.356$ \\
Original put          & $4.80{\times}10^{-1}$ & $0.5988$ & --- \\
Hard-constraint put   & $4.98{\times}10^{-2}$ & $0.9959$ & $0.0$ \\
Log-price put         & $3.69{\times}10^{-2}$ & $0.9978$ & $0.422$ \\
\bottomrule
\end{tabular}
\end{table}

\section{Multicollinearity diagnostics}\label{app:cond}
Standardization collapses the BS library condition number from $3.4{\times}10^6$ to $61.8$ on real SPY (and from $6.78{\times}10^4$ to $91.0$ on synthetic), making sparse regression numerically tractable.

\begin{table}[h]
\centering\footnotesize
\caption{Sparse-regression diagnostics on the synthetic BS library. Source: \texttt{ensemble\_sindy.csv}, \texttt{elastic\_net.csv}, \texttt{pca\_sindy.csv}.}
\begin{tabular}{lrrr}
\toprule
Term & Ensemble incl.\ prob. & Elastic-Net coef. & PCA-SINDy \\
\midrule
$V$                  & $0.00$ & $+0.0485$ & active \\
$\partial_S V$       & $0.00$ & $0.0000$  & active \\
$\partial_{SS} V$    & $\mathbf{1.00}$ & $0.0000$ & active \\
$S\partial_S V$      & $0.00$ & $-0.0496$ & active \\
$S^2\partial_{SS}V$  & $0.00$ & $-0.0201$ & active \\
\bottomrule
\end{tabular}
\end{table}

\section{Heston variance slicing}\label{app:heston}
\begin{table}[h]
\centering\footnotesize
\caption{Per-slice SINDy diffusion coefficient vs.\ $-\tfrac{1}{2}v$ ground truth. A linear fit yields slope $-0.5001$ (true: $-0.500$). Source: \texttt{heston\_variance\_slicing.csv}.}
\begin{tabular}{rrrr}
\toprule
$v$ & $\sigma{=}\sqrt{v}$ & true diff.\ coef. & discovered \\
\midrule
$0.01$ & $0.100$ & $-0.005$ & $-0.00516$ \\
$0.02$ & $0.141$ & $-0.010$ & $-0.01011$ \\
$0.04$ & $0.200$ & $-0.020$ & $-0.02010$ \\
$0.08$ & $0.283$ & $-0.040$ & $-0.04010$ \\
$0.16$ & $0.400$ & $-0.080$ & $-0.08015$ \\
\bottomrule
\end{tabular}
\end{table}

\section{Per-ticker contribution analysis}\label{app:contrib}
\begin{table}[h]
\centering\footnotesize
\caption{Fraction of $|\partial_t V|$ explained per term, per ticker. Source: \texttt{term\_contributions\_real.csv}.}
\begin{tabular}{lrrrr}
\toprule
Term & SPY & QQQ & AAPL & MSFT \\
\midrule
$V$                  & $0.053$ & $0.040$ & $0.038$ & $0.071$ \\
$\partial_S V$       & $0.469$ & $0.457$ & $0.424$ & $0.455$ \\
$\partial_{SS} V$    & $0.002$ & $0.001$ & $0.015$ & $0.001$ \\
$S\partial_S V$      & $0.476$ & $0.503$ & $0.505$ & $0.473$ \\
$S^2\partial_{SS}V$  & $0.000$ & $0.000$ & $0.019$ & $0.000$ \\
\bottomrule
\end{tabular}
\end{table}

\section{The analytical-$\theta$ fix: before vs.\ after}\label{app:tau-fix}
\begin{table}[h]
\centering\footnotesize
\caption{Real-data Dupire results before/after replacing FD $\partial_\tau C$ with analytical Black--Scholes $\theta$ evaluated pointwise from each grid point's own SVI-implied $\sigma_{\text{imp}}$. Sources: \texttt{improved\_real\_pipeline.csv}, \texttt{v4\_analytical\_theta\_results.csv}, \texttt{v5\_summary.csv}.}
\begin{tabular}{lrrrr}
\toprule
Metric & SPY & QQQ & AAPL & MSFT \\
\midrule
$R^2$ global 2-term (FD $\theta$)          & $-0.033$ & $-0.038$ & $-0.416$ & $-0.241$ \\
$R^2$ global 2-term (analytical $\theta$)  & $\mathbf{0.559}$ & $\mathbf{0.624}$ & $\mathbf{0.550}$ & $\mathbf{0.608}$ \\
Bootstrap CI width (FD $\theta$)           & $0.085$ & --- & --- & --- \\
Bootstrap CI width (analytical $\theta$)   & $\mathbf{0.016}$ & $\mathbf{0.019}$ & $\mathbf{0.020}$ & $\mathbf{0.030}$ \\
Per-expiration nonzero (analytical $\theta$) & $22/23$ & $20/20$ & $18/18$ & $15/15$ \\
\bottomrule
\end{tabular}
\end{table}

\section{Activation stability analysis}\label{app:stability}

Across five random seeds (42--46), we extract each of the two $[2,1]$ KAN-Dupire activations at 200 evaluation points on real SPY and compute pointwise 95\% confidence bands. The drift activation $\phi_{\text{drift}}(\partial_k C)$ has a band width of ${\approx}6\%$ of its dynamic range, indicating reproducible structure across seeds. The diffusion activation $\phi_{\text{diff}}(\partial_{kk} C)$ has a band width of ${\approx}60\%$ of its dynamic range: the curve shape varies substantially with initialization, so individual diffusion-edge interpretations should be reported with explicit uncertainty rather than treated as deterministic. Additional sensitivity studies vary the GP preprocessing kernel (RBF, Matern-3/2, Matern-5/2; \texttt{kernel\_sensitivity.csv}) and the L1 regularization strength ($0.001, 0.01, 0.1$; \texttt{regularization\_sensitivity.csv}); the drift edge remains stable in all cases. Source: \texttt{activation\_stability.csv}.

\section{Transfer experiments}\label{app:transfer}

We test three forms of generalization: cross-ticker (train on $T_1$, test on $T_2$), temporal (train on March 2026 snapshot, test on May 2026), and per-expiration leave-one-out. All results in \texttt{transfer\_experiments.csv} and \texttt{per\_expiration\_loo.csv}.

\begin{table}[h]
\centering\footnotesize
\caption{Transfer $R^2$ for the $[2,1]$ KAN-Dupire. Train and test sets are size $\sim\!600$ each. Source: \texttt{transfer\_experiments.csv}.}
\begin{tabular}{llrr}
\toprule
Experiment & Train $\to$ Test & $R^2_{\text{train}}$ & $R^2_{\text{test}}$ \\
\midrule
Cross-ticker & SPY $\to$ QQQ            & $0.650$ & $+0.107$ \\
Cross-ticker & QQQ $\to$ SPY            & $0.828$ & $-1.010$ \\
Cross-ticker & AAPL $\to$ MSFT          & $0.868$ & $-1.349$ \\
Cross-ticker & MSFT $\to$ AAPL          & $0.850$ & $+0.217$ \\
Temporal     & SPY (Mar) $\to$ SPY (May)& $0.650$ & $-3.513$ \\
Temporal     & QQQ (Mar) $\to$ QQQ (May)& $0.828$ & $-9.907$ \\
Maturity (mild) & SPY $\tau{<}0.75 \to \tau{\ge}0.75$ & $0.701$ & $-11.009$ \\
\bottomrule
\end{tabular}
\end{table}

Per-expiration LOO on SPY: $R^2$ rises with maturity through the short end, peaks at $0.81$ near $\tau{\approx}0.80$, and collapses monotonically toward $-10.57$ at $\tau{\approx}1.82$. The failure is concentrated at long maturities, consistent with the well-documented observation that short-dated and long-dated options have different effective dynamics. Time-varying coefficient models are the natural next step.

\section{Alternative smoothing comparison}\label{app:smoothing}

To check whether the discovery results are an artifact of SVI preprocessing, we re-run the GP $+$ analytical-$\theta$ pipeline on real SPY with two alternative implied-volatility smoothers.

\begin{table}[h]
\centering\footnotesize
\caption{Smoothing-method sensitivity on real SPY. Source: \texttt{smoothing\_comparison.csv}.}
\begin{tabular}{lrr}
\toprule
Smoothing method & $R^2$ (SPY) & median $\sigma_{\text{imp}}$ \\
\midrule
SVI (Gatheral)            & $\mathbf{+0.288}$ & $0.261$ \\
Cubic-spline per-expiry IV & $-0.439$ & $0.252$ \\
LOESS per-expiry IV        & $+0.237$ & $0.252$ \\
\bottomrule
\end{tabular}
\end{table}

SVI is the strongest of the three smoothers, but it is not invariant: cubic-spline collapses $R^2$ entirely. The findings depend on having a parametric arbitrage-aware surface model rather than pointwise IV smoothing.

\section{GP kernel selection per ticker}\label{app:gp-kernel}
\begin{table}[h]
\centering\footnotesize
\caption{RBF vs.\ Matern-$3/2$ GP-SINDy $R^2$ per ticker. Source: \texttt{gp\_kernel\_comparison.csv}.}
\begin{tabular}{lrrr}
\toprule
Ticker & $R^2$ (RBF) & $R^2$ (Matern) & winner \\
\midrule
SPY  & $0.9657$ & $0.6947$ & RBF \\
QQQ  & $0.9305$ & $0.9682$ & Matern \\
AAPL & $0.9233$ & $0.9963$ & Matern \\
MSFT & $0.5318$ & $0.9867$ & Matern \\
\bottomrule
\end{tabular}
\end{table}

\section{Computation costs}\label{app:cost}
\begin{table}[h]
\centering\footnotesize
\caption{Wall-clock per stage (s, CPU only). Source: \texttt{computation\_costs.csv}.}
\begin{tabular}{lr}
\toprule
Stage & seconds \\
\midrule
Full pipeline & $1753.4$ \\
PINN training (all variants) & $473.2$ \\
GP noise sweep (8 levels) & $11.3$ \\
Derivative-method comparison & $9.9$ \\
\bottomrule
\end{tabular}
\end{table}

\section{$\sigma_{\text{SINDy}}$ vs.\ IV-averaging baselines}\label{app:sigma-cmp}
Volume-weighted IV achieves rel.\ error $\{0.3\%, 1.1\%, 5.0\%, 1.1\%\}$ on SPY/QQQ/AAPL/MSFT for ATM $\sigma$ recovery; median IV achieves $\{0.7\%, 4.5\%, 0.8\%, 4.8\%\}$; SINDy (linear Dupire) achieves $\{12.7\%, 10.6\%, 6.1\%, 29.3\%\}$. SINDy's value on real data is structural discovery and nonlinearity detection, not raw point-estimate accuracy. Source: \texttt{sigma\_method\_comparison.csv}.

\section{Misspecification diagnostic taxonomy}\label{app:misspec-taxonomy}

We extend the single-point Merton experiment of \S\ref{sec:merton} into a structured sweep that probes how the BS-library SINDy coefficients deform under three families of misspecification: pure Merton jumps (sweep over intensity $\lambda$ and mean log-jump $\mu_J$) and Heston stochastic volatility (sweep over vol-of-vol $\sigma_v$). Source: \texttt{outputs/tables/misspec\_taxonomy.csv}, generated by \texttt{src/misspec\_taxonomy.py}.

\begin{table}[h]
\centering\footnotesize
\caption{Misspecification taxonomy: spurious $\partial_{SS}V$ activation under three families. The signature distinguishes jumps (small, linear-in-$\lambda$) from stochastic volatility (large, $24.5\times$ at matched stress).}
\label{tab:misspec-taxonomy}
\begin{tabular}{llrrr}
\toprule
Family & Parameter & Value & spurious $\partial_{SS}V$ & $R^2$ \\
\midrule
Jump intensity      & $\lambda$  & $0.05$ & $1.88$   & $1.000$ \\
Jump intensity      & $\lambda$  & $0.10$ & $3.23$   & $1.000$ \\
Jump intensity      & $\lambda$  & $0.20$ & $5.98$   & $1.000$ \\
Jump intensity      & $\lambda$  & $0.50$ & $14.48$  & $0.999$ \\
Jump size           & $\mu_J$    & $+0.10$ & $-22.37$ & $1.000$ \\
Stochastic vol      & $\sigma_v$ & $0.10$ & $60.83$  & $1.000$ \\
Stochastic vol      & $\sigma_v$ & $0.20$ & $110.07$ & $0.999$ \\
Stochastic vol      & $\sigma_v$ & $0.30$ & $\mathbf{146.47}$ & $0.996$ \\
Stochastic vol      & $\sigma_v$ & $0.50$ & $172.01$ & $0.988$ \\
\bottomrule
\end{tabular}
\end{table}

Two quantitative findings make this a taxonomy rather than a binary alarm. First, the intensity sweep is essentially linear: a least-squares fit of spurious $\partial_{SS}V$ on $\lambda$ over $\{0, 0.01, 0.05, 0.1, 0.2, 0.5\}$ yields slope $27.88$ with $R^2{=}0.9999$ (Figure~\ref{fig:misspec-jump}), so the spurious-coefficient magnitude can be read as a calibrated meter for jump intensity. Second, at matched ``visual'' stress on the surface ($\lambda{=}0.20$ vs.\ $\sigma_v{=}0.30$), the stochastic-vol misspecification produces a spurious coefficient $146.47 / 5.98 = 24.5\times$ larger than the jump misspecification. The discovery framework therefore distinguishes \emph{which family} of misspecification is present from the signature of the spurious activation. Figure~\ref{fig:misspec-stochvol} visualizes the qualitatively different deformation under Heston. We emphasize the claim is narrow: the spurious coefficient pattern is diagnostic of jump-vs-stochvol misspecification of the constant-$\sigma$ BS library, not a recovery of jump or Heston dynamics from option data.

\begin{figure}[h]
\centering
\includegraphics[width=0.62\linewidth]{figures/misspec_stochvol_pattern.pdf}
\caption{Heston ($\sigma_v$ sweep) deformation pattern: spurious $\partial_{SS}V$ saturates at $\sim\!172$, roughly $24.5\times$ the jump-misspecification magnitude at matched parameter stress. Source: \texttt{misspec\_taxonomy.csv}.}
\label{fig:misspec-stochvol}
\end{figure}

\section{Discovery-method baselines and 5-fold cross-validation}\label{app:cv}

\textbf{Discovery methods on the SVI-collinear 2-term library.} We benchmark four sparse-regression strategies and one closed-form baseline against the same SPY surface used in \S\ref{sec:realdata}. Source: \texttt{outputs/tables/discovery\_method\_comparison.csv}.

\begin{table}[h]
\centering\footnotesize
\caption{Discovery-method comparison on real SPY. The two sparsity-driven methods (weak-form regression and Ridge+threshold) zero out the diffusion coefficient on the 2-column SVI-collinear library; STLSQ-OLS is required to retain a diffusion term, and the KAN diagnostic stacks on top.}
\label{tab:discovery-methods}
\begin{tabular}{lrl}
\toprule
Method & $R^2$ (SPY) & Notes \\
\midrule
STLSQ (2-term GP-Dupire, OLS)         & $0.439$ & coefficients; baseline \\
\textbf{STLSQ $+$ $[2,1]$ KAN}        & $\mathbf{0.767}$ & activations; nonlinear residual \\
Weak-form regression (spectral basis) & $0.232$ & $c_2{=}-7.1\!\times\!10^{-7}$, $\sigma_{\text{loc}}$ undefined \\
Ridge $+$ threshold (RidgeCV)         & $0.080$ & $c_2$ thresholded to $0$ \\
Direct Dupire (pointwise, FD on GP)   & n/a (pointwise) & $\sigma_{\text{loc}}$ median $0.205$, IQR $0.200$ \\
\bottomrule
\end{tabular}
\end{table}

The two sparsity-driven methods (weak-form regression and Ridge+threshold) collapse the diffusion coefficient on this 2-column library because $\partial_k C$ and $\partial_{kk} C$ are nearly collinear after SVI smoothing --- any sparsity prior prefers the smaller-magnitude diffusion term to be exactly zero. STLSQ with OLS retains both columns, and the KAN diagnostic then captures the residual nonlinearity. Direct pointwise Dupire on the GP surface produces a defined local-volatility field on $80\%$ of cells with median $\sigma_{\text{loc}}{=}0.205$ but is not directly comparable on $R^2$.

\textbf{5-fold spatial cross-validation.} We split SPY into five spatial folds in $(k,\tau)$ and report mean$\pm$std of test $R^2$. Source: \texttt{outputs/tables/cv\_results.csv}, \texttt{cv\_kan\_activations\_summary.csv}.

\begin{table}[h]
\centering\footnotesize
\caption{5-fold spatial CV on SPY. KAN dominates on both in-sample fit and CV $R^2$.}
\label{tab:cv}
\begin{tabular}{lrr}
\toprule
Model & in-sample $R^2$ & CV $R^2$ (mean $\pm$ std) \\
\midrule
Linear Dupire (2-term) & $0.439$ & $0.289 \pm 0.037$ \\
$[2,1]$ KAN-Dupire     & $\mathbf{0.767}$ & $\mathbf{0.496 \pm 0.065}$ \\
\bottomrule
\end{tabular}
\end{table}

Across the five folds the learned KAN activations are highly consistent (mean pairwise correlation $0.989$ for the drift edge, $0.824$ for the diffusion edge), so the lift over linear Dupire is not a single-fit artifact. The lower diffusion-edge correlation matches the seed-sensitivity finding of Appendix~\ref{app:stability}: the diffusion activation shape is the seed/fold-dependent component of the diagnostic and should be reported with explicit uncertainty.

\section{Per-expiration coefficient analysis and regime transfer}\label{app:per-exp}

\textbf{Per-expiration coefficients vs.\ market IV term structure.} We fit linear Dupire per expiration on SPY and compare the discovered diffusion coefficient against the market-average implied volatility at that maturity. Source: \texttt{outputs/tables/per\_expiration\_coefficients.csv}; figure \texttt{figures/coefficient\_vs\_maturity.pdf}.

\begin{table}[h]
\centering\footnotesize
\caption{Per-expiration linear-Dupire coefficients on SPY vs.\ market average IV. The diffusion coefficient does not track the IV term structure (Spearman corr $= +0.008$) and turns negative for $\tau > 0.4$.}
\label{tab:per-exp}
\begin{tabular}{rrrrr}
\toprule
$\tau$ (yrs) & $n$ strikes & coef drift & coef diffusion & market avg IV \\
\midrule
$0.033$ & $73$ & $-0.425$ & $+0.0112$  & $0.240$ \\
$0.160$ & $46$ & $-0.204$ & $+0.0033$  & $0.205$ \\
$0.287$ & $19$ & $-0.136$ & $+0.0040$  & $0.191$ \\
$0.415$ & $31$ & $-0.142$ & $+0.0005$  & $0.198$ \\
$0.542$ & $14$ & $-0.149$ & $\mathbf{-0.0024}$ & $0.230$ \\
$0.797$ & $43$ & $-0.112$ & $\mathbf{-0.0003}$ & $0.214$ \\
$1.179$ & $5$  & $-0.102$ & $\mathbf{-0.0003}$ & $0.222$ \\
$1.815$ & $3$  & $-0.082$ & $\mathbf{-0.0003}$ & $0.214$ \\
\bottomrule
\end{tabular}
\end{table}

Two structural issues are visible. First, market average IV is U-shaped in maturity (short end $0.240$, interior $\approx 0.20$, long end $0.21$--$0.25$) but the discovered diffusion coefficient declines almost monotonically and changes \emph{sign} at $\tau \approx 0.4$. A local-variance interpretation $\sigma_{\text{loc}}^2 \propto c_2 / k^2_{\text{scale}}$ requires $c_2 > 0$; the long-maturity sign flip is structurally impossible for a true local-variance model. Second, the Spearman correlation between the per-expiration diffusion coefficient and market average IV is $+0.008$ --- the discovered diffusion does not encode the term structure the market prices. Together these point to a regression-identifiability collapse: at long maturities $\partial_{kk} C$ is small in magnitude relative to $\partial_k C$, and the OLS fit reallocates explanatory power across the two collinear columns, producing a sign-flipped diffusion coefficient that is not interpretable as a market quantity.

\textbf{Moneyness-regime transfer.} As an independent test of non-invariance we split SPY by $|k|$ into ATM ($|k| \le 0.05$) and OTM ($|k| > 0.05$) and train/test in both directions. Source: \texttt{outputs/tables/generalization\_analysis.csv}; figure \texttt{figures/transfer\_heatmap.pdf}.

\begin{table}[h]
\centering\footnotesize
\caption{ATM$\leftrightarrow$OTM regime transfer on SPY (split at $|k|{=}0.05$). Both directions fail; the asymmetry suggests separate operators for ATM and OTM rather than a single stationary Dupire.}
\label{tab:regime-transfer}
\begin{tabular}{lrrr}
\toprule
Direction & $R^2_{\text{train}}$ & $R^2_{\text{test}}$ & $n_{\text{train}} / n_{\text{test}}$ \\
\midrule
ATM $\to$ OTM & $0.499$ & $\mathbf{-0.888}$ & $150 / 450$ \\
OTM $\to$ ATM & $0.754$ & $\mathbf{-0.119}$ & $450 / 150$ \\
\bottomrule
\end{tabular}
\end{table}

The regime-transfer collapse complements the per-expiration finding: the operator is not stationary across moneyness either. The combined picture --- maturity sign-flip, near-zero IV correlation, and bi-directional regime-transfer failure --- supports the interpretation in \S\ref{sec:threats} that the in-sample $R^2$ lift on a single ticker reflects local fit on a slice-specific shape, not the discovery of a globally invariant local-volatility operator.

\section{Honest ablation: full chain and KAN-clean view}\label{app:ablation-kan}

Figure~\ref{fig:ablation} in the main text uses the grouped, non-monotonic ablation. The complementary ``KAN-clean'' view in Figure~\ref{fig:ablation-kan} adds each component in the natural order (GP $\to$ log-$m$ $\to$ analytical $\theta$ $\to$ KAN) and shows monotonic $R^2$ improvement at each step. Both views use the same SPY ablation chain (\texttt{ablation\_chain.csv}); the difference is presentation, and we keep both to avoid hiding the non-monotonicity that arises in configurations C and D before analytical $\theta$ is substituted.

\begin{figure}[h]
\centering
\includegraphics[width=0.68\linewidth]{figures/ablation_kan_clean.pdf}
\caption{KAN-clean ablation view: monotonic component-wise additions on SPY. Source: \texttt{ablation\_chain.csv}.}
\label{fig:ablation-kan}
\end{figure}

\end{document}
